import React from 'react';
import {Text} from 'react-native';
import {SafeAreaView} from 'react-native-safe-area-context';

const Loading2Screen = () => {
  return (
    <SafeAreaView>
      <Text>Loading2</Text>
    </SafeAreaView>
  );
};

export default Loading2Screen;
